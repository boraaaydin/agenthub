"use client";
type Props = { taskId: number; workitemId: number; isClosing: boolean; isCompleted: boolean; onConfirm: () => void; onDismiss: () => void };
export function TaskClosePrompt({ taskId, workitemId, isClosing, isCompleted, onConfirm, onDismiss }: Props) { if (isCompleted) return <p role="status" className="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">Task #{taskId} and workitem #{workitemId} were marked completed. The session was dismissed.</p>; return <section className="rounded-xl border border-sky-200 bg-sky-50 px-4 py-4"><h2 className="text-sm font-semibold text-sky-950">Execution finished</h2><p className="mt-1 text-sm text-sky-900">Mark task #{taskId} and workitem #{workitemId} as completed?</p><div className="mt-3 flex gap-3"><button type="button" onClick={onConfirm} disabled={isClosing} className="rounded-xl bg-sky-700 px-4 py-2 text-sm font-semibold text-white">{isClosing ? "Completing task…" : "Complete task and workitem"}</button><button type="button" onClick={onDismiss} disabled={isClosing} className="rounded-xl border border-slate-300 bg-white px-4 py-2 text-sm">Keep task open</button></div></section>; }
